{
  "name": "fufai-home",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/auth-utils.test.js test/backup-utils.test.js test/firestore-rules.test.js",
    "test:firestore": "node scripts/run-firestore-emulator-tests.mjs",
    "test:all": "npm test && npm run test:firestore"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase-tools": "^15.28.1"
  }
}
